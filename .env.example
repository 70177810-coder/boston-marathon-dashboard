# Boston Marathon Dashboard — Environment Variables
# Copy this file to .env and fill in values

# Port (auto-injected by Render/Railway — only set for local dev)
# PORT=8501

# Streamlit settings
STREAMLIT_SERVER_HEADLESS=true
STREAMLIT_BROWSER_GATHER_USAGE_STATS=false
